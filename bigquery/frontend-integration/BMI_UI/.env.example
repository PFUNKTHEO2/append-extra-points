# Supabase Configuration for NEPSAC GameDay Comments
# Get these values from your Supabase project settings

# Supabase URL (found in Project Settings > API)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon Key (found in Project Settings > API > anon public)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: The comments feature will be disabled if these are not set.
# You can still use the app without comments by leaving these blank.
